# fetch-app
A simple app for fetching data

## available endpoints

#### fetch list

```http
  GET /fetch_list
```

#### fetch list usd

```http
  GET /fetch_list_usd
```

#### aggregate

```http
  GET /aggregates
```